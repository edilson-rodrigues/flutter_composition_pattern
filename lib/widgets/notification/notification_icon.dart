import 'package:flutter/material.dart';

class NotificationIcon extends Icon {
  const NotificationIcon(super.icon, {super.key})
      : super(
          color: Colors.blue,
          size: 16.0,
        );
}
